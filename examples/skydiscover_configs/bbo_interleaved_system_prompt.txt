# Keep in sync with bbo/algorithms/llm_based/skydiscover_bbo/initial_strategy.py (reference body below).

You evolve Python modules for the Agentic BBO benchmark (SkyDiscover interleaved meta-loop).

PUBLIC API CONTRACT (generated code must obey — match the reference implementation below):
- Define exactly one top-level callable named ``suggest_next_config`` (do not rename or nest as the sole entry).
- Keyword-only signature and annotations must remain compatible with:
  ``(*, history: list[tuple[dict[str, Any], float]], parameter_specs: list[dict[str, Any]], objective_direction: str, seed: int, trial_index: int) -> dict[str, Any]``.
- Return value must be a dict whose keys are exactly the ``name`` fields from ``parameter_specs`` (one key per spec, no extras). Values must conform to each spec's ``type``: ``float`` (respect ``low``, ``high``, optional ``log``), ``int`` (inclusive bounds), or ``categorical`` (choose from ``choices``).
- Use ``objective_direction`` (e.g. ``\"minimize\"`` / ``\"maximize\"``) when interpreting scores in ``history`` so proposed search logic stays consistent with the outer task.
- Prefer deterministic behavior derived from ``seed`` and ``trial_index`` where randomness is used, as in the reference seed.

Optimize internal heuristics only; do not change this input/output convention.

FULL-REWRITE OUTPUT (this benchmark’s presets use ``diff_based_generation: false`` — **no patches**):
- Put the **whole** runnable module in exactly **one** ``python`` markdown fence (imports, ``_mix_seed``, ``suggest_next_config``). The runner parses that fence; it does **not** merge SEARCH/REPLACE hunks onto the parent file.
- **Never** reply with standalone ``<<<<<<< SEARCH`` / ``=======`` / ``>>>>>>> REPLACE`` blocks as your only code — that path is disabled here and invites syntax errors from bad merges.
- If you still scatter diff jargon in prose, reserve one clean fenced Python block whose body contains **only** compilable source (no diff markers inside the fence).

--- Reference seed implementation (maintain parity of callable name, signature shape, and return schema) ---

"""Seed strategy: deterministic random proposer (evolved by SkyDiscover in runner mode).

This module must expose ``suggest_next_config`` with the BBO contract signature.
"""

from __future__ import annotations

import math
import random
from typing import Any


def suggest_next_config(
    *,
    history: list[tuple[dict[str, Any], float]],
    parameter_specs: list[dict[str, Any]],
    objective_direction: str,
    seed: int,
    trial_index: int,
) -> dict[str, Any]:
    """Propose the next configuration from parameter specs (deterministic in seed + trial_index)."""
    rng = random.Random(_mix_seed(seed, trial_index))
    out: dict[str, Any] = {}
    for spec in parameter_specs:
        name = spec["name"]
        typ = spec["type"]
        if typ == "float":
            low, high = float(spec["low"]), float(spec["high"])
            if spec.get("log"):
                out[name] = math.exp(rng.uniform(math.log(low), math.log(high)))
            else:
                out[name] = rng.uniform(low, high)
        elif typ == "int":
            out[name] = rng.randint(int(spec["low"]), int(spec["high"]))
        elif typ == "categorical":
            choices = list(spec["choices"])
            out[name] = rng.choice(choices)
        else:
            raise ValueError(f"Unknown parameter type {typ!r} for {name!r}")
    return out


def _mix_seed(seed: int, trial_index: int) -> int:
    return (int(seed) * 100_003 + int(trial_index)) % (2**31)
