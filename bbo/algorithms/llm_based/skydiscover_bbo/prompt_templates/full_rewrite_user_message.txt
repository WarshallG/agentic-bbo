# Current Solution Information
- Main Metrics: 
{metrics}
- Focus areas: {improvement_areas}

# Program Generation History
## Previous Attempts

{previous_attempts}

{other_context_programs}

{current_program}

{search_guidance}

# Task
{task_objective}
{diversity_dimensions}
{diversity_note}

## Agentic BBO meta-evolution (full-file rewrite)

This run uses full-file rewrite only (not SEARCH/REPLACE). Bad diffs silently corrupt merges and cause SyntaxError on evaluation.

Respond with exactly ONE fenced block using the language tag below. Put the complete runnable module inside it: imports, helpers (e.g. _mix_seed), and def suggest_next_config with a valid body. Match the IO contract from the system prompt.

Do not output SEARCH/REPLACE markers (no <<<<<<< SEARCH, =======, >>>>>>> REPLACE) and do not use a diff-only fence as your answer.

The system values diversity (score, complexity) while raising COMBINED_SCORE.

Provide the full replacement program.

IMPORTANT: Keep the same inputs/outputs as the original benchmark contract; improve internals only.

```{language}
# Your rewritten full module here
```

**CRITICAL**: Write valid Python. Add concise docstrings on public functions where helpful.

{timeout_warning}
