# Background

Sysbench full-knob surrogate (196 dims). Offline sklearn RF; download ``SYSBENCH_all.joblib`` from the link in ``bbo/tasks/dbtune/assets/README.md``.
Full MySQL knob vector (196 active dimensions). Copy ``SYSBENCH_all.joblib``.
