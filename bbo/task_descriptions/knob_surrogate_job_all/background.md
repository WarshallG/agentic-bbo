# Background

JOB full-knob surrogate (196 dims). Offline sklearn RF; download ``JOB_all.joblib`` from the link in ``bbo/tasks/dbtune/assets/README.md``.
Full knob surrogate for JOB; copy ``JOB_all.joblib``.
