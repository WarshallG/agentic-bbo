# Constraints

- Keep every coordinate inside `[-5, 5]`.
- Evaluation `budget` must stay inside `[0.25, 1.0]`.
- Do not assume different budgets are directly comparable as if they were identical fidelities.
- Preserve append-only logging so replay and resume remain deterministic.
