# Domain Prior Knowledge

- Budget `1.0` removes the fidelity gap and returns the true sphere loss.
- Lower budgets inject a deterministic, structured distortion term instead of random noise.
- This task exists to validate benchmark plumbing around `suggestion.budget`, not to be a hard optimization challenge.
